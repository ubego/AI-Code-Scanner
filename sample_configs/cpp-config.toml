# Code Scanner Configuration for C++ Projects
# Human-focused checks - compilers and clang-tidy handle syntax/warnings.
#
# Copy this file to your project root as `config.toml`.

# ==============================================================================
# LLM Backend Configuration
# ==============================================================================

[llm]
# LM Studio (default)
backend = "lm-studio"   # Backend type: "lm-studio" or "ollama"
host = "localhost"       # Server hostname (usually localhost)
port = 1234              # Server port (LM Studio default: 1234, Ollama: 11434)
model = "qwen2.5-coder-7b-instruct"  # Optional for LM Studio
timeout = 600            # Request timeout in seconds
context_limit = 16384    # Context window size in tokens (minimum 16384 recommended)

# Ollama alternative (uncomment and comment out LM Studio above):
# backend = "ollama"     # Use Ollama backend
# host = "localhost"
# port = 11434           # Ollama default port
# model = "qwen3:4b"     # Model name (REQUIRED for Ollama)
# timeout = 600
# context_limit = 16384


# ==============================================================================
# Human-Focused Code Checks
# ==============================================================================

[[checks]]
pattern = "*.cpp, *.cc, *.cxx, *.hpp, *.h"
checks = [
    # Memory management checks
    "Check that stack allocation is used instead of heap allocation when an object's lifetime is predetermined",
    "Check that smart pointers (unique_ptr, shared_ptr) are used instead of raw pointers where appropriate.",
    "Check that RAII patterns are used instead of manual new/delete.",

    "Check for object slicing when copying derived classes.",
    "Check for dangling pointers after object destruction.",
    "Check for double-free vulnerabilities.",
    # Resource leak checks
    "Check that file handles are closed on all code paths.",
    "Check that network connections and sockets are properly released.",
    "Check that mutexes are unlocked on exception paths using lock_guard.",
    "Check that memory allocated in constructor is freed on partial construction failure.",
    # Concurrency checks
    "Check for data races on shared mutable state.",
    "Check for deadlock potential from inconsistent lock ordering.",
    "Check for missing memory barriers or atomic operations.",
    "Check that condition variables are used with proper predicates.",
    "Check for thread-unsafe initialization of local statics using pre-C++11 patterns.",
    # Undefined behavior checks
    "Check for signed integer overflow.",
    "Check for dereferencing nullptr or uninitialized pointers.",
    "Check for use-after-move of moved-from objects.",
    "Check for out-of-bounds array access.",
    "Check for modifying const objects through casts.",
    "Check for strict aliasing rule violations.",
    # Design checks

    "Check for circular dependencies between modules.",
    "Check for god classes with too many methods.",
    "Check for deeply nested inheritance hierarchies.",
    "Check for overuse of friend classes breaking encapsulation.",
    # Modern C++ checks
    "Check that STL algorithms are used instead of raw loops where appropriate.",
    "Check that static_cast or dynamic_cast is used instead of C-style casts.",
    "Check that std::string or string_view is used instead of manual string handling.",
    "Check that std::function is used instead of function pointers where clearer.",
    "Check that move operations are marked noexcept.",
    # Algorithm and data structure optimization checks
    "Check for O(n²) nested loops that could be optimized using hash maps (std::unordered_map) or sets (std::unordered_set).",
    "Check for linear searches in sorted containers that could use binary search (std::lower_bound, std::binary_search).",
    "Check for repeated string concatenation that could use std::string::reserve() or std::ostringstream.",
    "Check for unnecessary copying of large objects that could use const references or std::move.",
    "Check for std::vector that frequently resizes and could use reserve() with known capacity.",
    "Check for algorithms that could benefit from memoization or caching results.",
    "Check for data structures chosen without considering time/space trade-offs (e.g., std::list vs std::vector).",
    "Check for opportunities to use more efficient STL algorithms (e.g., std::sort, std::find_if, std::transform, std::accumulate).",
    "Check for containers where a different container type would be more efficient (vector vs deque vs list vs map).",
    "Check for sorting operations that could use more efficient comparison functions or key extraction.",
    "Check for std::map usage where std::unordered_map would be more efficient for non-ordered keys.",
    "Check for std::set usage where std::unordered_set would be more efficient for non-ordered keys.",
    "Check for repeated calls to .size() or .length() in loops that could be cached.",
    "Check for pass-by-value of large objects that should be passed by const reference.",
    "Check for algorithms with exponential complexity that could use dynamic programming.",
    "Check for priority queue use cases that could use std::priority_queue instead of manual sorting.",
    "Check for graph algorithms that could use more efficient data structures like adjacency lists.",
    "Check for cache-friendly data access patterns that could improve performance.",
]

[[checks]]
pattern = "CMakeLists.txt, *.cmake"
checks = [
    # CMake best practices
    "Check that target-based commands (target_link_libraries, target_include_directories) are used instead of directory-based commands.",
    "Check that modern CMake (3.x) patterns are used instead of legacy patterns.",
    "Check that PRIVATE, PUBLIC, INTERFACE keywords are used correctly for target properties.",
    "Check that find_package() is used with REQUIRED or proper error handling.",
    "Check that CMAKE_CXX_STANDARD is set instead of manually adding -std=c++ flags.",
    # CMake organization
    "Check that CMakeLists.txt files are not overly long and are split into modules where appropriate.",
    "Check that hardcoded paths are avoided in favor of CMake variables and generator expressions.",
    "Check that install() commands are provided for libraries and executables meant for distribution.",
    "Check that option() is used for configurable build options with sensible defaults.",
    # CMake correctness
    "Check that dependencies between targets are correctly specified.",
    "Check that source files are not GLOB'd in a way that misses new files.",
    "Check that include guards or include_guard() are used in .cmake modules.",
    "Check that FetchContent or find_package is used consistently for external dependencies.",
]

[[checks]]
pattern = "*"
checks = [
    "Check for circular header dependencies.",

    "Check for dead code or unused files.",
    "Check for feature flags for features that have shipped.",
    "Check for duplicate implementations.",
    "Check for repeated code blocks that can be extracted into a separate function.",
    "Check that all changed code paths handle edge cases and error conditions correctly.",
    "Check that the overall design fits the existing architecture and abstractions.",
    "Check that complex logic is broken down into small, understandable units with clear responsibilities.",
    "Check that names of variables, methods, and classes are clear, descriptive, and consistent.",
    "Check for obvious performance issues like N+1 queries or unnecessary loops.",
    "Check that database queries and external service calls are efficient and properly constrained.",
    "Check that public APIs, configuration changes, or new behaviors are properly documented.",
    "Check that comments and documentation are accurate, necessary, and up to date.",
    "Check that logging and error messages are clear, informative, and follow project conventions.",
    "Check that no sensitive data (keys, PII) is logged or exposed in output.",
    "Check that configuration changes have safe, sensible defaults.",
    "Check that feature flags are named consistently and reflect their purpose.",
]

# ==============================================================================
# Ignore Patterns (files and directories to skip during scanning)
# ==============================================================================
# Files and directories matching these patterns with empty checks will be ignored.
# Use /*dirname*/ syntax for directory patterns.
# Add or modify patterns as needed for your project.

[[checks]]
pattern = "*.md, *.txt, *.rst, *.html, *.json, *.toml, *.yaml, *.yml, /*test*/, /*tests*/, /*external*/, /*3rdparty*/, /*third_party*/, /*vendor*/, /*build*/, /*cmake-build-*/"
checks = []
